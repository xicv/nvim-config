-- lua/custom/plugins/misc.lua
-- Nothing left here after the May-2026 cleanup pass (go.nvim removed —
-- gopls + treesitter cover daily Go work). Kept as a stub so init.lua's
-- `{ import = 'custom.plugins.misc' }` doesn't break when you re-add things.
return {}
